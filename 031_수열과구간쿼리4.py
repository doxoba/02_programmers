# ---------------------------------------------------------
# 031 : 수열과 구간 쿼리 4  [반복문]
# https://school.programmers.co.kr/learn/courses/30/lessons/181922
# ---------------------------------------------------------
# 문제:
# 정수 배열 `arr`와 2차원 정수 배열 `queries`이 주어집니다. `queries`의 원소는 각각 하나의 `query`를 나타내며, `[s, e, k]` 꼴입니다.
# 각 `query`마다 순서대로 `s` ≤ `i` ≤ `e`인 모든 `i`에 대해 `i`가 `k`의 배수이면 `arr[i]`에 1을 더합니다.
# 위 규칙에 따라 `queries`를 처리한 이후의 `arr`를 return 하는 solution 함수를 완성해 주세요.
# ----------------------------------------
# [제한사항]
# - 1 ≤ `arr`의 길이 ≤ 1,000
# 0 ≤ `arr`의 원소 ≤ 1,000,000
# - 1 ≤ `queries`의 길이 ≤ 1,000
# 0 ≤ `s` ≤ `e` < `arr`의 길이
# 0 ≤ `k` ≤ 5
# ----------------------------------------
# [입출력 예]
# arr | queries | result
# [0, 1, 2, 4, 3] | [[0, 4, 1],[0, 3, 2],[0, 3, 3]] | [3, 2, 4, 6, 4]
# ----------------------------------------
# [입출력 예 설명]
# 입출력 예 #1
# - 각 쿼리에 따라 `arr`가 다음과 같이 변합니다.
# arr
# [0, 1, 2, 4, 3]
# [1, 2, 3, 5, 4]
# [2, 2, 4, 5, 4]
# [3, 2, 4, 6, 4]
# - 따라서 [3, 2, 4, 6, 4]를 return 합니다.
# ----------------------------------------
# ※ 2023년 04월 27일 입출력 예 설명이 수정되었습니다.
# ---------------------------------------------------------
# (여기에 내 풀이)
# 입력: arr, queries
# 출력: arr  # 배열 리턴
# 어떻게: i가 k의 배수이면(i % k == 0) arr[i]에 1을 더한다

def solution(arr, queries):
#    answer = 0  # 숫자가 아닌 배열을 꺼내는 거라 answer 지정 X
    for i in queries:
        s, e, k = i
        for i in range(s,e+1):
            if i % k == 0:
                arr[i] += 1
    return(arr)


# 정수 배열 `arr`와 2차원 정수 배열 `queries`이 주어집니다. `queries`의 원소는 각각 하나의 `query`를 나타내며, `[s, e, k]` 꼴입니다.
# 각 `query`마다 순서대로 `s` ≤ `i` ≤ `e`인 모든 `i`에 대해 `i`가 `k`의 배수이면 `arr[i]`에 1을 더합니다.
# 위 규칙에 따라 `queries`를 처리한 이후의 `arr`를 return 하는 solution 함수를 완성해 주세요.

# 내 오답
# def solution(arr, queries):
#    answer = 0
#    for i in queries:
#        s, e, k = i
#        for i in range(s,e):
#            if arr[i] % k == 0:
#                answer = arr[i] + 1
#    return answer

# 메모:
# -----------------------------------------------
# ❓ 질문 31 : 수열과 구간 쿼리 4
# -----------------------------------------------
# 😵 페인포인트 :
#    1. 배수 조건 수식 방향 혼동 (k % i vs i % k)
#    2. 인덱스(위치번호) vs 값 개념 혼동
#       - for i in list    -> i = 값 자체
#       - for i in range() -> i = 인덱스 (위치번호)
#    3. arr[i] 수정 방법 혼동 (append vs +=)
#       - append() : 리스트 끝에 새 항목 추가
#       - arr[i] += 1 : 해당 위치 값에 1 더하기
#    4. range(s, e) vs range(s, e+1) - e 포함 여부
#    5. return 대상 혼동 (arr[1] vs arr)
# -----------------------------------------------
# 💡 풀이 :
#
#    def solution(arr, queries):
#        for i in queries:           # queries에서 [s, e, k] 하나씩 꺼내기
#            s, e, k = i             # query 분해
#            for i in range(s, e+1): # s~e 인덱스 순회 (e 포함이라 +1)
#                if i % k == 0:      # 인덱스가 k의 배수이면
#                    arr[i] += 1     # 그 위치 값에 1 더하기
#        return arr
# -----------------------------------------------
# ✅ 팁 :
#    - "i가 k의 배수" -> i % k == 0  (i를 k로 나눈 나머지가 0)
#    - 값을 수정해야 할 땐 인덱스가 필요 -> range() 사용
#    - append()는 리스트 끝에 추가, += 은 해당 위치 값 수정
#    - range(s, e+1) : e까지 포함하려면 항상 +1
#    - return 은 배열 전체 (arr), 특정 값 하나(arr[i]) 아님
# -----------------------------------------------
# 🌍 실생활 응용 :
#    이 문제 구조 = "조건에 맞는 항목만 골라서 업데이트"
#
#    실제 상황                              코드 구조
#    ------------------------------------   ----------------------------
#    특정 카테고리 상품 가격 일괄 할인     -> 조건 맞는 상품만 price += 할인액
#    특정 직급 직원만 보너스 지급          -> 조건 맞는 직원만 salary += 보너스
#    게임에서 특정 구간 몬스터 체력 강화   -> 조건 맞는 몬스터만 hp += 1
#
#    for 대상 in 전체목록:
#        조건 분해
#        for 범위 안 항목:
#            if 조건 맞으면:
#                값 업데이트
# -----------------------------------------------
