# ---------------------------------------------------------
# 046 : 문자열의 앞의 n글자  [문자열]
# https://school.programmers.co.kr/learn/courses/30/lessons/181907
# ---------------------------------------------------------
# 문제:
# 문자열 `my_string`과 정수 `n`이 매개변수로 주어질 때, 
# `my_string`의 앞의 `n`글자로 이루어진 문자열을 return 하는 solution 함수를 작성해 주세요.
# ----------------------------------------
# [제한사항]
# - `my_string`은 숫자와 알파벳으로 이루어져 있습니다.
# - 1 ≤ `my_string`의 길이 ≤ 1,000
# - 1 ≤ `n` ≤ `my_string`의 길이
# ----------------------------------------
# [입출력 예]
# my_string | n | result
# "ProgrammerS123" | 11 | "ProgrammerS"
# "He110W0r1d" | 5 | "He110"
# ----------------------------------------
# [입출력 예]
# 입출력 예 #1
# - 예제 1번의 `my_string`에서 앞의 11글자는 "ProgrammerS"이므로 이 문자열을 return 합니다.
# 입출력 예 #2
# - 예제 2번의 `my_string`에서 앞의 5글자는 "He110"이므로 이 문자열을 return 합니다.
# ---------------------------------------------------------
# (여기에 내 풀이)

# 문자열 `my_string`과 정수 `n`이 매개변수로 주어질 때, 
# `my_string`의 앞의 `n`글자로 이루어진 문자열을 return 하는 solution 함수를 작성해 주세요.

# 입력: my_string, n
# 출력: 
# 어떻게: 

# 메모:
def solution(my_string, n):
    return my_string[:n]


print(solution("ProgrammerS123",11))

# -----------------------------------------------
# ❓ 질문 : 문자열 my_string과 정수 n이 주어질 때,
#          my_string의 앞의 n글자로 이루어진 문자열을 반환하는
#          solution 함수 작성
# 😵 페인포인트 : 오랜만에 다시 접해서 슬라이싱 문법이 바로 안 떠오름
#                → [:n] 형태로 시작 인덱스를 생략할 수 있다는 게 헷갈림
# 💡 풀이 :
#    1. 최종 결과 형태 파악 — "문자열 앞부분 n글자만 잘라낸 문자열"
#    2. 파이썬 슬라이싱 [:n] 활용 — 시작 위치를 생략하면 자동으로 처음부터 적용됨
#    3. solution 함수의 return에 그대로 적용
#
#    def solution(my_string, n):
#        return my_string[:n]
#
#    print(solution("ProgrammerS123", 11))   # 'ProgrammerS'
#
# ✅ 팁 :
#    - [:n]은 "처음부터 n번째 직전까지"라는 뜻, 인덱스 직접 계산 불필요
#    - 반대로 뒤에서부터 n글자 자르고 싶으면 [-n:] 사용
#    - 이 문제는 반복문/컴프리헨션 없이 슬라이싱 한 줄로 끝나는 유형
# -----------------------------------------------