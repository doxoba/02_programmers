# ---------------------------------------------------------
# 039 : 9로 나눈 나머지  [문자열]
# https://school.programmers.co.kr/learn/courses/30/lessons/181914
# ---------------------------------------------------------
# 문제:
# 음이 아닌 정수를 9로 나눈 나머지는 그 정수의 각 자리 숫자의 합을 9로 나눈 나머지와 같은 것이 알려져 있습니다.
# 이 사실을 이용하여 음이 아닌 정수가 문자열 `number`로 주어질 때, 
# 이 정수를 9로 나눈 나머지를 return 하는 solution 함수를 작성해주세요.
# ----------------------------------------
# [제한사항]
# - 1 ≤ `number`의 길이 ≤ 100,000
# - `number`의 원소는 숫자로만 이루어져 있습니다.
# - `number`는 정수 0이 아니라면 숫자 '0'으로 시작하지 않습니다.
# ----------------------------------------
# [입출력 예]
# number | result
# "123" | 6
# "78720646226947352489" | 2
# ----------------------------------------
# [입출력 예 설명]
# 입출력 예 #1
# - 예제 1번의 `number`는 123으로 각 자리 숫자의 합은 6입니다. 6을 9로 나눈 나머지는 6이고, 실제로 123 = 9 × 13 + 6입니다. 따라서 6을 return 합니다.
# 입출력 예 #2
# - 예제 2번의 `number`는 78720646226947352489으로 각자리 숫자의 합은 101입니다. 101을 9로 나눈 나머지는 2이고, 실제로 78720646226947352489 = 9 × 8746738469660816943 + 2입니다. 따라서 2를 return 합니다.
# ---------------------------------------------------------
# (여기에 내 풀이)
# 입력: 문자열 number
# 출력: 나머지
# 어떻게: number을 9로 나눠서
def solution(number):
    total = 0
    for i in number:
        i = int(i)
        total += i
    return total % 9

print(solution("123"))

# 메모:
# -----------------------------------------------
# ❓ 질문 39 : 문자열로 주어진 숫자를 9로 나눈 나머지 구하기
# 😵 페인포인트 : 문자열을 int()로 통째로 바꾸려 함
#                → "각 자리 숫자의 합"이라는 조건을 놓침
#                → for문을 언제 써야 하는지 감이 안 잡힘
# 💡 풀이 :
#    1. number는 문자열 → for문으로 한 글자씩 꺼냄
#    2. 꺼낸 글자(문자)를 int()로 숫자로 변환
#    3. total에 누적해서 더함
#    4. 최종 total을 9로 나눈 나머지 return
#
#    def solution(number):
#        total = 0
#        for i in number:
#            i = int(i)
#            total += i
#        return total % 9
#
# ✅ 팁 :
#    - 문제에 "각각", "각 자리", "모든", "하나씩" → for문 신호!
#    - 문자열은 for문으로 한 글자씩 꺼낼 수 있음 ("123" → "1","2","3")
#    - int()는 통째로 바꾸면 for문 못 씀 → 반드시 for문 안에서 변환
#    - 순서: 문자열 유지 → for문 → int() 변환 → 누적
# -----------------------------------------------